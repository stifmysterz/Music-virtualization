# Claude Code replacement brief

在当前 Music Visualisation 项目中实施增量替换。严格遵守以下约束：

## 不可破坏的现有功能

1. 保留完整的 2D Visualizer，包括入口、控制项、音频响应和预设。
2. 保留全部 61+ 3D Effects，包括效果注册表、切换逻辑、参数和兼容层；修改前后统计效果数量，修改后不得减少。
3. 保留完整的 VJ Loops，包括素材发现、选择、循环播放及其 UI。
4. 不得新增、修改、移动、重命名或删除以下目录中的任何文件：
   - `source/`
   - `assets/`
   - `shaders/`

## 工作流程

1. 先建立当前行为与文件清单，记录 2D Visualizer、3D Effects 数量、VJ Loops 的入口和验证方式。
2. 仅修改实现目标所必需、且不在受保护目录中的文件。
3. 不使用全量脚手架覆盖项目；不执行 destructive reset、clean 或批量删除。
4. 如果所需修改必须触碰受保护目录，立即停止并说明原因，不要绕过限制。
5. 完成后运行现有测试、构建和静态检查，并做以下回归验证：
   - 2D Visualizer 可打开并对音频响应。
   - 3D Effects 可枚举，数量不少于修改前且不少于 61。
   - VJ Loops 可选择并连续循环。
   - 受保护目录的修改前后哈希完全一致。
6. 最终报告列出修改文件、验证结果、3D Effects 前后数量，以及受保护目录哈希对比。

## 受保护路径规则

路径匹配不区分大小写，并同时保护目录本身及所有后代文件。任何补丁或生成文件一旦命中 `source`、`assets` 或 `shaders` 顶层目录，必须拒绝。

